<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:orientation="vertical"
              android:paddingTop="12dp"
              android:paddingBottom="12dp"
              android:layout_width="match_parent"
              android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/list_item_image_bitmap"
        android:src="@drawable/common_google_signin_btn_icon_dark_normal"
        android:contentDescription="@string/Label"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:scaleType="fitStart"
        android:layout_width="128dp"
        android:layout_height="128dp" />

    <TextView
            android:id="@+id/list_item_image_label"
            android:text="@string/Select_floor"
            android:textSize="@dimen/text_size"
            android:textStyle="bold"
            android:layout_alignParentTop="true"
            android:layout_toRightOf="@id/list_item_image_bitmap"
            android:layout_toEndOf="@id/list_item_image_bitmap"
            android:padding="8dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

    <TextView
            android:id="@+id/list_item_image_uuid"
            android:text="image uuid"
            android:textSize="@dimen/text_small_size"
            android:textColor="@color/colorAccent"
            android:textStyle="bold"
            android:layout_below="@id/list_item_image_label"
            android:layout_toRightOf="@id/list_item_image_bitmap"
            android:layout_toEndOf="@id/list_item_image_bitmap"
            android:layout_alignParentRight="true"
            android:layout_alignParentEnd="true"
            android:layout_marginTop="4dp"
            android:padding="8dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

</RelativeLayout>